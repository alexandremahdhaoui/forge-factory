name: factory-lang-python
kind: mcp-server
version: 0.1.0
description: Render Python manifests from the workspace composition.
openapi:
  specPath: ../../api/factory.v1.yaml
generate:
  packageName: main
  docsBaseURL: https://raw.githubusercontent.com/alexandremahdhaoui/forge-factory/refs/heads/main
surface:
  tools:
    - name: language
      description: Report which language this engine speaks.
      input: RenderInput
      output: LanguageOutput
    - name: render
      description: Render every python manifest the workspace needs.
      input: RenderInput
      output: RenderOutput
    - name: describe
      description: Describe the runtime this language's members exec - artifacts, layout, env, prerequisites. Pure data.
      input: DescribeInput
      output: RuntimeDescription
